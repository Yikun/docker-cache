name: "Schedule master"

on:
  push:
    branches:
    - '**'

jobs:
  call-build-and-test:
    name: Run
    uses: ./.github/workflows/test.yml
    with:
      java: 8
      branch: master
      cron: true
